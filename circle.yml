---
dependencies:
  pre:
    - bash ./install_SDL2.sh
  post:
    - sudo apt-get install -y x11vnc
    - x11vnc -forever -nopw:
        background: true
test:
  override:
    # - bundle install
    - bash ./test.sh
